# คู่มือการใช้งานระบบจัดการคิวงาน (Admin Manual) - วัดโพธิ์แจ้

## 1. การเปิดระบบ (Start Server)
เพื่อให้ระบบทำงานได้สมบูรณ์ (แจ้งเตือน LINE, เก็บข้อมูลลงไฟล์) จำเป็นต้องเปิดโปรแกรมจำลอง Server ก่อนครับ

**วิธีเปิด (สำหรับ Python):**
1. เปิดหน้าจอ Command Prompt (cmd)
2. พิมพ์คำสั่งแล้วกด Enter:
   ```cmd
   python app.py
   ```
3. เมื่อขึ้นข้อความว่า `Python Flask Server running on http://localhost:5000` แปลว่าระบบพร้อมใช้งานแล้ว (ห้ามปิดหน้าจอนี้ระหว่างใช้งาน)

---

## 2. การเข้าสู่ระบบหลังบ้าน
1. เปิดไฟล์ `admin.html` หรือเข้าผ่านลิงก์ **"🔐 ระบบจัดการภายใน"** ที่ด้านล่างสุดของหน้าเว็บไซต์หลัก
2. ใส่รหัสผ่าน: **1234** แล้วกด "เข้าสู่ระบบ"

---

## 3. การใช้งานหน้าจัดการ (Dashboard)

### 3.1 ตารางรายการคิวงาน
หน้าแรกจะแสดงรายการจองทั้งหมด โดยเรียงตามวันที่และเวลา
- **สถานะ [ออนไลน์]:** รายการที่โยมจองเข้ามาผ่านหน้าเว็บ
- **สถานะ [ออฟไลน์]:** รายการที่ทางวัดลงข้อมูลเอง (รับกิจนิมนต์ทางโทรศัพท์/มาติดต่อเอง)
- **วันคงเหลือ:** ระบบจะคำนวณให้อัตโนมัติว่าอีกกี่วันจะถึงวันงาน

### 3.2 การเพิ่มคิวงาน (Manual Booking)
ใช้สำหรับกรณีรับกิจนิมนต์ทางโทรศัพท์ หรือต้องการ **"ล็อกคิว"** ไม่ให้ว่าง
1. กรอกข้อมูลในช่องด้านบน:
   - **ชื่อเจ้าภาพ/เหตุผล:** เช่น "คุณสมชาย (โทรจอง)" หรือ "วันพระงดรับกิจ"
   - **วันที่:** เลือกวันที่ต้องการ
   - **เวลา:** เลือกรอบเวลา
   - **จำนวนพระ:** ระบุจำนวนรูป
   - **สถานที่:** ระบุสถานที่
2. กดปุ่ม **"ยืนยันเพิ่มคิว"**
3. รายการจะเข้าไปรวมในระบบ และตัดโควตาหน้าเว็บทันที

### 3.3 การลบคิวงาน (Cancel Booking)
- กดปุ่ม **"ลบ"** สีแดงเพื่อยกเลิกรายการ
- **ระบบป้องกัน 7 วัน:** หากงานนั้นเหลือเวลา **น้อยกว่า 7 วัน** ปุ่มลบจะกลายเป็นสีเทาและกดไม่ได้ (ตามกฎระเบียบวัด)
  - *วิธีแก้ไขกรณีฉุกเฉิน:* สามารถลบข้อมูลในไฟล์ `bookings.json` โดยตรงได้ (ควรทำโดยผู้ดูแลระบบเทคนิค)

---

## 4. กฎระเบียบและโควตา (Quota Logic)
ระบบถูกตั้งค่าไว้ดังนี้:
- **รอบ 06.30 น.:** รับได้ **1 งาน** (เต็มแล้วหน้าเว็บจะจองไม่ได้)
- **รอบอื่นๆ:** รับได้ **2 งาน**
- **การแจ้งเตือน:** เมื่อมีคนจองผ่านเว็บ ระบบจะส่ง LINE Notify เข้ากลุ่มทันที (ต้องตั้งค่า Token ในไฟล์ `app.py`)

---

## 5. การแก้ไขข้อมูลแจ้งเตือน LINE
หากต้องการเปลี่ยนกลุ่มไลน์ที่จะให้แจ้งเตือน:
1. เปิดไฟล์ `app.py` ด้วยโปรแกรม Notepad
2. แก้ไขบรรทัดที่เขียนว่า:
   `LINE_NOTIFY_TOKEN = 'ใส่รหัสTokenของคุณที่นี่'`
3. บันทึกไฟล์ (Save) และปิด/เปิด Server ใหม่ (ทำข้อ 1 ใหม่)

---

## 6. การดูแลและอัปเดตเว็บไซต์ (Web Maintenance)

### 6.1 การอัปเดตทั่วไป (แก้ไขคำผิด/เปลี่ยนรูป)
1. **แก้ไข:** แก้ไขไฟล์ในเครื่องคอมพิวเตอร์ตามปกติ
2. **ตรวจสอบ:** ดับเบิ้ลคลิก `run_server.bat` แล้วเข้า `http://localhost:5000` เพื่อดูผลลัพธ์
3. **อัปโหลด:** หากพอใจแล้ว ให้ดับเบิ้ลคลิกไฟล์ **`update_site.bat`** (ที่ Desktop) เพื่อส่งขึ้นเว็บจริง

### 6.2 การแก้ไขระบบใหญ่ (Major Update)
หากมีการแก้ไขโครงสร้างใหญ่ เช่น เปลี่ยนหน้าตาเว็บทั้งหมด หรือแก้ระบบคำนวณโควตา:
1. **สำรองข้อมูล (Backup):** ให้ Copy โฟลเดอร์งานเก็บไว้ในชื่ออื่นก่อน (เช่น `Web-project-backup`) กันพลาด
2. **ทำในเครื่องเสมอ:** ห้ามพยายามแก้ไฟล์บน GitHub โดยตรง ให้แก้ในเครื่องและทดสอบให้มั่นใจ 100%
3. **ทยอยทำ:** อย่าแก้รวดเดียวเยอะๆ ให้แก้ทีละส่วนแล้วลองรันดูว่าผ่านไหม
4. **ถ้าพัง:** ให้ก๊อปปี้ไฟล์จากตัว Backup มาทับ หรือใช้คำสั่ง Git เพื่อย้อนกลับ (ควรปรึกษาผู้เชี่ยวชาญ)

**ข้อควรระวังสำหรับ GitHub Pages:**
- เว็บไซต์จริงจะทำงานแบบ **Static** (ไม่มี Python Server)
- หากแก้โค้ด Python (`app.py`) จะมีผลเฉพาะตอนรันในเครื่องเท่านั้น ไม่ส่งผลต่อหน้าเว็บออนไลน์
